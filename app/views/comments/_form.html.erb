<h6>我要留言！</h6>

<%= form_with model: @comment, url: event_comments_path(@event.id),  local: true do |f| %>
<%#= form_with model: [@event, @comment], local: true do |f| %>

  <div class="form-group">
    <label for="name">暱稱：
        <%= f.text_field :name, class: 'form-control', id: "bootstrap-overrides", input_html: {value: comment_name } %>
      
    </label>
  </div>

  <div class="form-group">
    <label for="body">留言：
      <div class="form-group">
        <%= f.text_area :body, class: 'form-control', id: "bootstrap-overrides" %>
      </div>
    </label>
  </div>

  <%= f.submit "送出", class: "btn btn-warning btn-sm" %>
<% end %>
